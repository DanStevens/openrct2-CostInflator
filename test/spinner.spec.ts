class Spinner {
  public name: "Spinner"
}

describe("Spinner", () => {
  describe("Creation", () => {
    it("Should TBD", () => {
      const objUT = new Spinner();
      expect(objUT).toEqual({
        name: "Spinner"
      });
    });
  });
});
